spring:
  application:
    name: ai_agent
  datasource:
    url: jdbc:h2:mem:llmdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        "[format_sql]": true

  h2:
    console:
      enabled: true
      path: /h2-console
  ai:
    model:
      chat: none
    openai:
      api-key: ${OPENAI_API_KEY:unused-for-mock-provider}
      chat:
        model: gpt-4o-mini

app:
  llm:
    provider: mock
